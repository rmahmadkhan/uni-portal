# Example environment for production-like runs
#
# Usage:
#   export $(cat .env | xargs)   # simplistic; or use your platform's env manager
#   cd app
#   DJANGO_SETTINGS_MODULE=university_portal.settings_prod python manage.py check --deploy
#
# Never commit real secrets.

DJANGO_SETTINGS_MODULE=university_portal.settings_prod

# Required in production
DJANGO_SECRET_KEY=replace-with-a-long-random-secret
DJANGO_ALLOWED_HOSTS=example.com,www.example.com

# Recommended: Postgres
DATABASE_URL=postgres://portal_user:portal_pass@127.0.0.1:5432/portal_db

# Recommended
DJANGO_SITE_URL=https://example.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://example.com,https://www.example.com

# Optional toggles
DJANGO_SECURE_SSL_REDIRECT=1
DJANGO_SESSION_COOKIE_SECURE=1
DJANGO_CSRF_COOKIE_SECURE=1
DJANGO_SECURE_HSTS_SECONDS=31536000
DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS=1
DJANGO_SECURE_HSTS_PRELOAD=1
DJANGO_SECURE_PROXY_SSL_HEADER=1

# Optional: if you terminate TLS at a proxy and run app over HTTP internally,
# keep DJANGO_SECURE_SSL_REDIRECT=1 and ensure X-Forwarded-Proto=https is set.
